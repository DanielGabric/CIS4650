/* 
   1. Have a non-void function not return anything.
   2. return an expression from a void function.
   3. Call a function with too many parameters.
*/


int gcd(int u,int v){
    if ( v == 0 ) return u;
    else return gcd(v,u-u/v*v);

}

int f(void){
    
}

void main(void){

    int x; int y; 
 
    x = input(); y = input(2);
    output(gcd(x,y)); 
    return 1; 
    
}